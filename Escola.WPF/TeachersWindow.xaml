<Window x:Class="Escola.WPF.TeachersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Escola.WPF"
        mc:Ignorable="d"
        Title="TeachersWindow" Height="450" Width="800">
 <Grid>
     <Grid.RowDefinitions>
         <RowDefinition Height="Auto"/>
         <RowDefinition Height="*"/>
     </Grid.RowDefinitions>

     <!-- Título -->
     <TextBlock Text="Teachers Page" FontSize="20" FontWeight="Bold" Margin="15,0,0,15" />
     <!-- Formulário de Input -->
     <StackPanel Grid.Row="0" Margin="10">
         <TextBlock Text="Professor Name" Margin="0,5"/>
         <TextBox Name="txtProfessorName" Width="300" Height="30" FontSize="14" Margin="0,5"/>

         <TextBlock Text="Phone" Margin="0,5"/>
         <TextBox Name="txtProfessorPhone" Width="300" Height="30" FontSize="14" Margin="0,5"/>

         <TextBlock Text="Email" Margin="0,5"/>
         <TextBox Name="txtProfessorEmail" Width="300" Height="30" FontSize="14" Margin="0,5"/>

         <TextBlock Text="Teaching Area" Margin="0,5"/>
         <TextBox Name="txtProfessorTeachingArea" Width="300" Height="30" FontSize="14" Margin="0,5"/>

         <!-- Botões de Ação -->
         <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,10">
             <Button Name="btnCreateProfessor" Content="Create" Width="100" Margin="0,5" Click="btnCreateProfessor_Click"/>
             <Button Name="btnEditProfessor" Content="Update" Width="100" Margin="0,5" Click="btnEditProfessor_Click"/>
             <Button Name="btnDeleteProfessor" Content="Delete" Width="100" Margin="0,5" Click="btnDeleteProfessor_Click"/>
         </StackPanel>
     </StackPanel>

     <!-- DataGrid para exibição dos Professores -->
        <DataGrid x:Name="dgProfessors" Grid.Row="1" Margin="10" AutoGenerateColumns="False" 
               SelectionMode="Single" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" 
               IsReadOnly="True" SelectionChanged="dgProfessors_SelectionChanged">

            <DataGrid.Columns>
            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
            <DataGridTextColumn Header="Full name" Binding="{Binding FullName}" Width="*"/>
            <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="*"/>
            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
            <DataGridTextColumn Header="Teaching Area" Binding="{Binding TeachingArea}" Width="*"/>
        
        </DataGrid.Columns>
        </DataGrid>



    </Grid>
 
</Window>
