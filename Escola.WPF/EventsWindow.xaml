<Window x:Class="Escola.WPF.EventsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Escola.WPF"
        mc:Ignorable="d"
        Title="EventsWindow" Height="450" Width="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- 0: Title -->
            <RowDefinition Height="Auto"/>
            <!-- 1: Form -->
            <RowDefinition Height="Auto"/>
            <!-- 2: Combos -->
            <RowDefinition Height="Auto"/>
            <!-- 3: Buttons -->
            <RowDefinition Height="*"/>
            <!-- 4: DataGrid-->
        </Grid.RowDefinitions>

       

        <!-- Reorganized form -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Column 1 -->
            <StackPanel Grid.Column="0" Margin="5">
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Name:" Width="100"/>
                    <TextBox Name="txtName" Width="200" Height="30" FontSize="14"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Event date:" Width="100"/>
                    <TextBox Name="dpEventDate" Width="200" Height="30" FontSize="14"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Event hour:" Width="100"/>
                    <TextBox Name="txtEventTime" Width="200" Height="30" FontSize="14"/>
                </StackPanel>
            </StackPanel>

            <!-- Column 2 -->
            <StackPanel Grid.Column="1" Margin="5">
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Location:" Width="100"/>
                    <TextBox Name="txtLocation" Width="200" Height="30" FontSize="14"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Description:" Width="100"/>
                    <TextBox Name="txtDescription" Width="200" Height="30" FontSize="14"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- ComboBoxes -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,5,0,10">
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label Content="Select Student:" Width="100"/>
                <ComboBox Name="cbStudents" Width="150" DisplayMemberPath="FullName" SelectedValuePath="Id"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label Content="Select Teacher:" Width="100"/>
                <ComboBox Name="cbTeachers" Width="150" DisplayMemberPath="FullName" SelectedValuePath="Id"/>
            </StackPanel>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Left" Margin="0,0,0,15" Width="1200">
            <Button Name="btnCreate" Content="Create" Width="100" Margin="5" Click="btnCreate_Click"/>
            <Button Name="btnEdit" Content="Update" Width="100" Margin="5" Click="btnEdit_Click"/>
            <Button Name="btnDelete" Content="Delete" Width="100" Margin="5" Click="btnDelete_Click"/>
            <Button Content="Add Teacher" Width="120" Margin="5" Click="btnAssociateTeacher_Click"/>
            <Button Content="Add Student" Width="120" Margin="5" Click="btnAssociateStudent_Click"/>
            <Button Content="Remove Student" Width="120" Margin="5" Click="btnRemoveStudent_Click"/>
            <Button Content="Remove Teacher" Width="120" Margin="5" Click="btnRemoveTeacher_Click"/>
            <Button Content="⬅ Come back to main menu"
                Click="BackToMenu_Click"
                Margin="10"
                Padding="10,5"
                FontWeight="Bold"
                Background="#FF007ACC"
                Foreground="White"
                Cursor="Hand"
                BorderThickness="0"
                Width="180"
                Height="40"/>
        </StackPanel>
    



    <!-- DataGrid -->
        <DataGrid Name="dgEvents" Grid.Row="4" AutoGenerateColumns="False" SelectionMode="Single" IsReadOnly="True" SelectionChanged="dgEvents_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Event Date" Binding="{Binding EventDate, StringFormat='dd/MM/yyyy'}" Width="*"/>
                <DataGridTextColumn Header="Event Hour" Binding="{Binding EventTime}" Width="*"/>
                <DataGridTextColumn Header="Location" Binding="{Binding Location}" Width="*"/>
                <DataGridTextColumn Header="Students" Binding="{Binding StudentNames}" Width="*"/>
                <DataGridTextColumn Header="Teachers" Binding="{Binding TeacherNames}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
