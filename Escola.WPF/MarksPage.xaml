<Page x:Class="Escola.WPF.MarksPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:Escola.WPF"
      Title="MarksPage" Height="420" Width="636">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Text="Marks Page" FontSize="20" FontWeight="Bold" Margin="0,0,0,15" />

        <!-- Formulário de entrada -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="5">
                <TextBlock Text="Student ID:"/>
                <TextBox Name="txtStudentId" Width="150" Margin="0,5,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="5">
                <TextBlock Text="Subject ID:"/>
                <TextBox Name="txtSubjectId" Width="150" Margin="0,5,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Margin="5">
                <TextBlock Text="Assessment Type:"/>
                <TextBox Name="txtAssessmentType" Width="150" Margin="0,5,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="3" Margin="5">
                <TextBlock Text="Score:"/>
                <TextBox Name="txtScore" Width="150" Margin="0,5,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="4" Margin="5">
                <TextBlock Text="Assessment Date:"/>
                <DatePicker Name="dpAssessmentDate" Width="150" Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>

        <!-- Botões -->
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Left" Margin="0,0,0,15">
            <Button Name="btnCreate" Content="Create" Width="100" Margin="5" Click="btnCreate_Click"/>
            <Button Name="btnEdit" Content="Edit" Width="100" Margin="5" Click="btnEdit_Click"/>
            <Button Name="btnDelete" Content="Delete" Width="100" Margin="5" Click="btnDelete_Click"/>
        </StackPanel>

        <!-- Tabela de notas -->
        <DataGrid Name="dgMarks"
                  Grid.Row="3"
                  AutoGenerateColumns="False"
                  SelectionMode="Single"
                  IsReadOnly="True"
                  Margin="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                <DataGridTextColumn Header="Student" Binding="{Binding StudentName}" Width="*"/>
                <DataGridTextColumn Header="Subject" Binding="{Binding SubjectName}" Width="*"/>
                <DataGridTextColumn Header="Assessment Type" Binding="{Binding AssessmentType}" Width="*"/>
                <DataGridTextColumn Header="Assessment Date" Binding="{Binding AssessmentDate}" Width="*"/>
                <DataGridTextColumn Header="Score" Binding="{Binding Grade}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>