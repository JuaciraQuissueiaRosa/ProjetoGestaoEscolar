<Window x:Class="Escola.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Escola.WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="598" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="34*"/>
            <RowDefinition Height="63*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical" Margin="10,10,10,10" Grid.RowSpan="2">
            <Button Content="Load Students"  Click="BtnLoadStudents_Click" Margin="5"/>
            <Button Content="Load Subjects" Click="BtnLoadSubjects_Click" Margin="5"/>
            <Button Content="Load Teachers" Click="BtnLoadTeachers_Click" Margin="5"/>
            <Button Content="Load Classes"  Click="BtnLoadClasses_Click" Margin="5"/>
            <Button Content="Load Marks" Click="BtnLoadMarks_Click" Margin="5"/>
            <Button Content="Load TimeTables" Click="BtnLoadTimeTables_Click" Margin="5"/>
            <Button Content="Load GradeSheets" Click="BtnLoadGradeSheets_Click" Margin="5"/>
            <Button Content="Load Events" Click="BtnLoadEvents_Click" Margin="5"/>
            <DataGrid x:Name="myDataGrid" AutoGenerateColumns="True" Height="400" Margin="5" />
        </StackPanel>

        <!-- Frame para carregar as páginas -->
        <Frame Name="MainFrame" NavigationUIVisibility="Hidden" Margin="220,10,10,10" Grid.RowSpan="2"/>
    </Grid>
</Window>
