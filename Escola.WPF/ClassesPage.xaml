<Page x:Class="Escola.WPF.ClassesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Escola.WPF"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TurmasPage">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="509*"/>
            <ColumnDefinition Width="271*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Text="Classes Page" FontSize="20" FontWeight="Bold" Margin="15,0,0,15" />
        
        <!-- Input Form for Class Details -->
        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,10" Grid.ColumnSpan="2">
            <TextBlock Text="Class Name" Margin="0,5"/>
            <TextBox Name="txtClassName" Width="200" Margin="0,5"/>

            <TextBlock Text="Group Name" Margin="0,5"/>
            <TextBox Name="txtGroupName" Width="200" Margin="0,5"/>

            <StackPanel Margin="5">
                <TextBlock Text="Academic Year:" />
                <TextBox Name="txtAcademicYear" Width="150" />
            </StackPanel>

            <TextBlock Text="Schedule" Margin="0,5"/>
            <TextBox Name="txtSchedule" Width="200" Margin="0,5"/>

            <TextBlock Text="Select Student" Margin="0,10,0,5"/>
            <ComboBox Name="cbStudents" Width="200" DisplayMemberPath="FullName" SelectedValuePath="Id"/>

            <TextBlock Text="Select Teacher" Margin="0,10,0,5"/>
            <ComboBox Name="cbTeachers" Width="200" DisplayMemberPath="FullName" SelectedValuePath="Id"/>

            <TextBlock Text="Select Subject" Margin="0,10,0,5"/>
            <ComboBox Name="cbSubjects" Width="200" DisplayMemberPath="Name" SelectedValuePath="Id"/>

            <Button Content="Associate to Class" Width="200" Margin="0,10,0,0" Click="btnAssociate_Click"/>

            <Button Content="Remover da Turma" Click="btnRemoveFromClass_Click" Width="200" Margin="5"/>

        </StackPanel>

        <!-- Classes DataGrid -->
        <DataGrid Name="dgClasses"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  SelectionMode="Single"
                  IsReadOnly="True"
                  Margin="0,10,0,0" Grid.ColumnSpan="2" SelectionChanged="dgClasses_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                <DataGridTextColumn Header="Class Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Group Name" Binding="{Binding Course}" Width="*"/>
                <DataGridTextColumn Header="Schedule" Binding="{Binding Shift}" Width="*"/>
                <DataGridTextColumn Header="Academic Year" Binding="{Binding AcademicYear}" Width="*"/>
                <DataGridTextColumn Header="Students" Binding="{Binding StudentNames}" Width="*"/>
                <DataGridTextColumn Header="Subjects" Binding="{Binding SubjectNames}" Width="*"/>
                <DataGridTextColumn Header="Teachers" Binding="{Binding TeacherNames}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Action Buttons -->
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Left" Margin="225,10,0,0" Grid.ColumnSpan="2">
            <Button Name="btnCreate" Content="Create" Width="100" Margin="5" Click="btnCreate_Click"/>
            <Button Name="btnEdit" Content="Edit" Width="100" Margin="5" Click="btnEdit_Click"/>
            <Button Name="btnDelete" Content="Delete" Width="100" Margin="5" Click="btnDelete_Click"/>
        </StackPanel>
    </Grid>
</Page>
