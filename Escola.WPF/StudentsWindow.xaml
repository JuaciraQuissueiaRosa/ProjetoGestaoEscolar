<Window x:Class="Escola.WPF.StudentsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Escola.WPF"
        mc:Ignorable="d"
        Title="StudentsWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

    

       
        <!-- Formulário de Entrada de Dados -->
        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,10">
            <TextBlock Text="Full name" Margin="0,5"/>
            <TextBox Name="txtFullName" Width="300" Height="28" FontSize="12" Margin="0,5"/>

            <TextBlock Text="Birth Date" Margin="0,5"/>
            <DatePicker Name="dpBirthDate" Width="300" Height="28" FontSize="12" Margin="0,5"/>

            <TextBlock Text="Phone" Margin="0,5"/>
            <TextBox Name="txtPhone" Width="300" Height="28" FontSize="12" Margin="0,5"/>

            <TextBlock Text="Address" Margin="0,5"/>
            <TextBox Name="txtAddress" Width="300" Height="28" FontSize="12" Margin="0,5"/>

            <TextBlock Text="Email" Margin="0,5"/>
            <TextBox Name="txtEmail" Width="300" Height="28" FontSize="12" Margin="0,5"/>

            <TextBlock Text="Search" Margin="0,5"/>
            <TextBox Name="txtSearch" Width="300" Height="28" FontSize="12" Margin="0,5"/>
        </StackPanel>

        <!-- Tabela de Alunos -->
        <DataGrid Name="dgStudents"
                Grid.Row="1"
                AutoGenerateColumns="False"
                SelectionMode="Single"
                IsReadOnly="True"
                Margin="0,10,0,0" SelectionChanged="dgStudents_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                <DataGridTextColumn Header="Full name" Binding="{Binding FullName}" Width="*"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="Birth Date" Binding="{Binding BirthDate, StringFormat=dd/MM/yyyy}" Width="*"/>
                <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Botões -->
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Name="btnCreate" Content="Create" Width="100" Margin="5" Click="btnCreate_Click"/>
            <Button Name="btnEdit" Content="Update" Width="100" Margin="5" Click="btnEdit_Click"/>
            <Button Name="btnDelete" Content="Delete" Width="100" Margin="5" Click="btnDelete_Click"/>

            <Button Content="Search" Width="80" Margin="5" Click="btnSearch_Click"/>

            <Button Name="btnHistory" Content="History" Width="100" Margin="5" Click="btnHistory_Click"/>
            <Button Content="⬅ Come back to menu"
    Click="BackToMenu_Click"
    HorizontalAlignment="Right"
    VerticalAlignment="Top"
    Margin="10"
    Padding="10,5"
    FontWeight="Bold"
    Background="#FF007ACC"
    Foreground="White"
    Cursor="Hand"
    BorderThickness="0"
    Width="140"
    Height="40"/>
        </StackPanel>
    </Grid>
</Window>
