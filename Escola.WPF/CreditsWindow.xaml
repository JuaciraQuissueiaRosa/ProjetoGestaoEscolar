<Window x:Class="Escola.WPF.CreditsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Escola.WPF"
        mc:Ignorable="d"
        Title="CreditsWindow" Height="450" Width="800">
    <Grid Background="#1E1E2F">

        <!-- Button  -->
        <Button Content="⬅ Come back to menu"
             Click="BackToMenu_Click"
             HorizontalAlignment="Left"
             VerticalAlignment="Top"
             Margin="10"
             Padding="10,5"
             FontWeight="Bold"
             Background="#FF007ACC"
             Foreground="White"
             Cursor="Hand"
             BorderThickness="0"
             Width="140"
             Height="40"/>
        
        <!-- Title-->
        <TextBlock Text="Credits"
                FontSize="32"
                FontWeight="Bold"
                Foreground="White"
                HorizontalAlignment="Center"
                Margin="0,40,0,0"
                VerticalAlignment="Top"/>

        <!-- StackPanel with informations -->
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <TextBlock Text="Developed by: Juacira Rosa"
                    Foreground="#FFA500"
                    FontSize="20"
                    FontWeight="SemiBold"
                    HorizontalAlignment="Center"
                 VerticalAlignment="Center"/>

        <TextBlock Text="Version 1.0.0"
                    Foreground="#CCCCCC"
                    FontSize="16"
                    HorizontalAlignment="Center"
                  VerticalAlignment="Bottom"/>

        <TextBlock Text="2025/05/24"
                    Foreground="#888888"
                    FontSize="14"
                    HorizontalAlignment="Center"
                 VerticalAlignment="Stretch"/>


        <!-- Canvas opcional para efeito (caso animado) -->
        <Canvas Name="ConfettiCanvas" IsHitTestVisible="true"/>
    </Grid>
</Window>
