<Window x:Class="Escola.WPF.TimetableWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Escola.WPF"
        mc:Ignorable="d"
        Title="TimetableWindow" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

     

        <!-- Form -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Column -->
            <StackPanel Grid.Column="0" Margin="10">
                <StackPanel Orientation="Horizontal" Margin="5">
                    <Label Content="Class:" Width="100"/>
                    <ComboBox x:Name="txtClassId" Width="200" DisplayMemberPath="Name" SelectedValuePath="Id"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="5">
                    <Label Content="Subject:" Width="100"/>
                    <ComboBox x:Name="txtSubjectId" Width="200" DisplayMemberPath="Name" SelectedValuePath="Id"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="5">
                    <Label Content="Teacher:" Width="100"/>
                    <ComboBox x:Name="txtTeacherId" Width="200" DisplayMemberPath="FullName" SelectedValuePath="Id"/>
                </StackPanel>
            </StackPanel>

            <!-- Column -->
            <StackPanel Grid.Column="1" Margin="10">
                <StackPanel Orientation="Horizontal" Margin="5">
                    <Label Content="Day of Week:" Width="120"/>
                    <TextBox x:Name="txtDayOfWeek" Width="180"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="5">
                    <Label Content="Start Time:" Width="120"/>
                    <TextBox x:Name="txtStartTime" Width="180"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="5">
                    <Label Content="End Time:" Width="120"/>
                    <TextBox x:Name="txtEndTime" Width="180"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- Table -->
        <DataGrid x:Name="dgTimeTable" Grid.Row="1" Margin="0,10"
              AutoGenerateColumns="False" CanUserAddRows="False"
              SelectionMode="Single" IsReadOnly="True"
              SelectionChanged="dgTimeTable_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                <DataGridTextColumn Header="Class" Binding="{Binding ClassName}" Width="*"/>
                <DataGridTextColumn Header="Subject" Binding="{Binding SubjectName}" Width="*"/>
                <DataGridTextColumn Header="Teacher" Binding="{Binding TeacherName}" Width="*"/>
                <DataGridTextColumn Header="Day" Binding="{Binding DayOfWeek}" Width="*"/>
                <DataGridTextColumn Header="Start" Binding="{Binding StartTime}" Width="*"/>
                <DataGridTextColumn Header="End" Binding="{Binding EndTime}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button x:Name="btnCreate" Content="Create" Width="100" Margin="5" Click="btnCreate_Click"/>
            <Button x:Name="btnEdit" Content="update" Width="100" Margin="5" Click="btnEdit_Click"/>
            <Button x:Name="btnDelete" Content="Delete" Width="100" Margin="5" Click="btnDelete_Click"/>
            <Button Content="⬅ Come back to menu" Click="BackToMenu_Click"
                Margin="10" Padding="10,5" FontWeight="Bold"
                Background="#FF007ACC" Foreground="White"
                Cursor="Hand" BorderThickness="0" Width="140" Height="40"/>
        </StackPanel>
    </Grid>

</Window>
